import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6030FF",
    },
    secondary: {
      main: "#0D30AB",
    },
  },
});

export default theme;
